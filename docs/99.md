The implementation is correct; the test expects the wrong exception.

After delivery fails, `release_otp_request()` clears `otp_requested_at` but preserves:

- The valid challenge
- The bound email
- Retry eligibility

Therefore `get_otp_verification()` correctly raises `OtpNotRequested`, not `InvalidLinkChallenge`.

Change the test import:

```python
from notes_bot.otp_state import (
    OtpNotRequested,
    get_otp_verification,
)
```

Then change:

```python
with pytest.raises(InvalidLinkChallenge):
```

to:

```python
with pytest.raises(
    OtpNotRequested,
    match="Request an OTP",
):
    get_otp_verification(
        path,
        challenge.token,
        "user@example.com",
    )
```

This is an important security property: a transient Supabase/network error must not let the same link switch to another email.

One additional correction: the traceback shows this in `get_otp_verification()`:

```python
with open_database(database_path) as connection:
```

That form does not close the SQLite connection. Replace it with:

```python
with database_connection(database_path) as connection:
```

and update the import accordingly. Check for any remaining instances:

```bash
rg -n "with open_database" src tests
```

Expected: no output.

Now validate:

```bash
uv run pytest tests/test_supabase_otp.py -v
uv run pytest -W error::ResourceWarning
uv run ruff format .
uv run ruff check .
uv run pytest
git diff --check
```

Expected: all three OTP-service tests and the full suite pass. Then we can implement `verify_code()` and atomic encrypted-session storage.
