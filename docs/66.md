Everything is clean locally: formatting, linting, and all 27 regular tests pass. The integration test is intentionally skipped unless its required environment is enabled.

Now commit and push the formatting fix:

```bash
cd ../..

git add apps/notes-cli/src/notes_cli/config.py
git commit -m "Format Notes CLI source"
git push origin main
```

Watch CI:

```bash
gh run watch
```

Or open the repository’s **Actions** tab on GitHub.

Once the workflow is green:

```bash
git status --short
```

If that produces no output, create and push the release tag:

```bash
git tag -a notes-cli-v0.1.0 \
  -m "Notes CLI v0.1.0"

git push origin notes-cli-v0.1.0
```

Then verify:

```bash
git tag --list 'notes-cli-v*'
git ls-remote --tags origin 'notes-cli-v0.1.0'
```

That completes the first stable CLI milestone.
